apiVersion: apps/v1
kind: Deployment
metadata:
  name: arenax-backend
  namespace: arenax
  labels:
    app: arenax-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: arenax-backend
  template:
    metadata:
      labels:
        app: arenax-backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/roastellar/arenax-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: arenax-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: arenax-secrets
                  key: jwt-secret
          envFrom:
            - configMapRef:
                name: arenax-config
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: arenax-backend
  namespace: arenax
spec:
  selector:
    app: arenax-backend
  ports:
    - port: 80
      targetPort: http
  type: ClusterIP
